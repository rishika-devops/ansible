- name: declaring and printing varaiables
  hosts: localhost
  vars:
   NAME: "sowjanya"
   WISHES: "good morning"
  tasks:
  - name: variables
    ansible.builtin.debug:
     msg: "hello $NAME........$WISHES"